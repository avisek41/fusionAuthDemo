import {
  logIn,
  logOut,
  selectCurrentUserId,
  selectCurrentToken,
  setCredentials,
} from './AuthSlice';
export { default as AuthSlice } from './AuthSlice';

export { logIn, logOut, selectCurrentUserId, selectCurrentToken, setCredentials };

